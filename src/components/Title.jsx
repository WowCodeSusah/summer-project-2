import React from 'react';

const Title = () => {

    // Plantiful
    // Schelude
    // FriendZone

    const TitleText = "Hello"

    return (
        <>{TitleText}</>
    );
};

export default Title;